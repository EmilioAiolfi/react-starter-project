// Libs
import React from 'react';


const Home = React.createClass({
  render() {
    return (
      <section id="home">
        <h2>{ 'Home' }</h2>
      </section>
    );
  }
});

export default Home;
